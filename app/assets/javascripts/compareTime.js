function compareTime(transferZones) {
	// sortedTransferZones = [];
	if (transferZones.length == 2) {
		t1 = transferZones[0];
		t2 = transferZones[1];
		console.log(t1);
		console.log(t2);
	}
	// return sortedTransferZones;
}