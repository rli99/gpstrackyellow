<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>


<h1>This is Gmap page</h1>

<div style='width: 800px;'>
  <div id="map" style='width: 800px; height: 400px;'></div>
</div>

<script type = 'text/javascript'>

 data = <%=raw @hash.to_json %>;

console.log(data)

// var myLatLng = {lat: -25.363, lng: 131.044};

  var map = new google.maps.Map(document.getElementById('map'), {
    zoom: 4,
    center: data[0]
  });

  var marker = new google.maps.Marker({
    position: data[0],
    map: map,
    title: 'Hello World!'
});

     var marker = new google.maps.Marker({
    position: data[1],
    map: map,
    title: 'Hello World!'
  });


// handler = Gmaps.build('Google');
// handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){

// 	markers = handler.addMarkers(data, {draggable: true});
// 	polylines = handler.addPolyline(<%=raw @hash.to_json %>, {draggable: true});

//     handler.bounds.extendWith(markers);
//   	handler.fitMapToBounds();

//   	google.maps.event.addListener(handler.getMap(), 'click', function(event) {
// 	    console.log("mother fucker point!");
// 	    console.log(polylines);
// 	    polylines.getPath().push(event,latLng);
//     });

// });



</script>